<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
             xmlns:tools="http://schemas.android.com/tools"
             android:layout_width="match_parent"
             android:layout_height="match_parent"
             tools:context="com.example.georg.radiostreameralt.RecordFragment">

    <!-- TODO: Update blank fragment layout -->

    <Button
        android:text="Recording Now"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true"
        android:id="@+id/record_now_button"
        android:background="@android:color/holo_blue_dark"
        android:layout_toStartOf="@+id/schedule_button"
        android:elevation="2dp"/>

    <Button
        android:text="Rec Folder"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/folder_button"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:background="@android:color/holo_blue_dark"
        android:elevation="2dp"
        android:layout_toEndOf="@+id/schedule_button"/>

    <Button
        android:text="Scheduled "
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/schedule_button"
        android:background="@android:color/holo_blue_dark"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:elevation="2dp"/>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/record_now_button"
        android:id="@+id/record_layout_for_fragments">

    </RelativeLayout>
</RelativeLayout>
